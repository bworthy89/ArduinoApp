<?xml version="1.0" encoding="utf-8"?>
<!-- MainWindow.xaml - Main application window with navigation -->
<Window
    x:Class="ArduinoConfigApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Arduino Configuration App">

    <Grid>
        <NavigationView x:Name="NavView"
                        PaneDisplayMode="Left"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="True"
                        SelectionChanged="NavView_SelectionChanged">

            <!-- Navigation Menu Items -->
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Dashboard" Tag="Dashboard">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE80F;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Content="Inputs" Tag="Inputs">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE961;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Content="Displays" Tag="Displays">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE7F4;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Content="Output Mapping" Tag="OutputMapping">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE92E;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Content="Testing" Tag="Testing">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE9D9;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Content="Wiring Guide" Tag="Wiring">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8B1;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <!-- Footer Items -->
            <NavigationView.FooterMenuItems>
                <NavigationViewItem Content="Generate Code" Tag="GenerateCode">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE943;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>

            <!-- Header with project name and connection status -->
            <NavigationView.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Project Name -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="{x:Bind ViewModel.ProjectName, Mode=OneWay}"
                                   Style="{StaticResource SubtitleTextBlockStyle}"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="*"
                                   Style="{StaticResource SubtitleTextBlockStyle}"
                                   Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                   Visibility="{x:Bind ViewModel.UnsavedChangesVisibility, Mode=OneWay}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Connection Status and Actions -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                        <!-- Connection Indicator -->
                        <Border Background="{x:Bind ViewModel.ConnectionBackgroundBrush, Mode=OneWay}"
                                CornerRadius="10"
                                Padding="12,6">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="8" Height="8"
                                         Fill="{x:Bind ViewModel.ConnectionIndicatorBrush, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind ViewModel.ConnectedPort, Mode=OneWay, TargetNullValue='Not Connected'}"
                                           Margin="8,0,0,0"
                                           FontSize="12"/>
                            </StackPanel>
                        </Border>

                        <!-- Theme Toggle -->
                        <Button Command="{x:Bind ViewModel.ToggleThemeCommand}"
                                ToolTipService.ToolTip="Toggle Theme">
                            <FontIcon Glyph="&#xE793;" FontSize="16"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </NavigationView.Header>

            <!-- Content Frame -->
            <Frame x:Name="ContentFrame"/>
        </NavigationView>

        <!-- Status Bar -->
        <Border VerticalAlignment="Bottom"
                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,0"
                Padding="16,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                           VerticalAlignment="Center"
                           FontSize="12"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="16">
                    <TextBlock VerticalAlignment="Center" FontSize="12">
                        
                        <Run Text="Board: "/><Run Text="{x:Bind ViewModel.BoardDisplayText, Mode=OneWay}"/>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
